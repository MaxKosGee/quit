# language code for the application
quit.language=de

# country code for the application
quit.country=DE

# standard target language to translate 
quit.defaultTargetLanguage=de

# standard source language to translate 
quit.defaultSourceLanguage=en

# defines if the result is automatically copied to the clipboard
quit.autoCopyResult=false